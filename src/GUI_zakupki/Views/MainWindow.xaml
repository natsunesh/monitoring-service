<Window x:Class="GUI_zakupki.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:GUI_zakupki.Helpers"
        Title="Монитор закупок"
        Height="650"
        Width="900"
        Background="#F5F7FA"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip">

	<Window.Resources>
		<local:ServiceStatusBrushConverter x:Key="ServiceStatusBrushConverter"/>

		<DropShadowEffect x:Key="WindowShadow"
                          Color="#33000000"
                          BlurRadius="18"
                          ShadowDepth="2"
                          Opacity="0.5"/>

		<Style TargetType="Button">
			<Setter Property="Background" Value="#00897B"/>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="FontSize" Value="13"/>
			<Setter Property="Padding" Value="12,6"/>
			<Setter Property="BorderThickness" Value="0"/>
			<Setter Property="Cursor" Value="Hand"/>
			<Setter Property="Margin" Value="0,0,6,0"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
							<ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"
                                              TextElement.Foreground="{TemplateBinding Foreground}"
                                              TextElement.FontSize="{TemplateBinding FontSize}"
                                              TextElement.FontWeight="{TemplateBinding FontWeight}"/>
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter Property="Background" Value="#00695C"/>
							</Trigger>
							<Trigger Property="IsPressed" Value="True">
								<Setter Property="Background" Value="#004D40"/>
							</Trigger>
							<Trigger Property="IsEnabled" Value="False">
								<Setter Property="Background" Value="#B2DFDB"/>
								<Setter Property="Foreground" Value="#E0F2F1"/>
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<Style TargetType="TextBox">
			<Setter Property="FontSize" Value="13"/>
			<Setter Property="Padding" Value="8,5"/>
			<Setter Property="BorderBrush" Value="#B2DFDB"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="Background" Value="White"/>
			<Setter Property="Foreground" Value="#263238"/>
			<Setter Property="Margin" Value="0,0,0,6"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="TextBox">
						<Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
							<ScrollViewer x:Name="PART_ContentHost" Margin="2"/>
						</Border>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<Style TargetType="ListBox">
			<Setter Property="Background" Value="White"/>
			<Setter Property="BorderBrush" Value="#B2DFDB"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="FontSize" Value="13"/>
			<Setter Property="Padding" Value="4"/>
			<Setter Property="MinHeight" Value="120"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="ListBox">
						<Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
							<ScrollViewer Focusable="False" Padding="{TemplateBinding Padding}">
								<ItemsPresenter/>
							</ScrollViewer>
						</Border>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
			<Setter Property="ItemContainerStyle">
				<Setter.Value>
					<Style TargetType="ListBoxItem">
						<Setter Property="Padding" Value="8,5"/>
						<Setter Property="Background" Value="Transparent"/>
						<Setter Property="Foreground" Value="#263238"/>
						<Style.Triggers>
							<Trigger Property="IsSelected" Value="True">
								<Setter Property="Background" Value="#E0F2F1"/>
								<Setter Property="Foreground" Value="#004D40"/>
							</Trigger>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter Property="Background" Value="#F5F5F5"/>
							</Trigger>
						</Style.Triggers>
					</Style>
				</Setter.Value>
			</Setter>
		</Style>

		<Style TargetType="CheckBox">
			<Setter Property="FontSize" Value="14"/>
			<Setter Property="Foreground" Value="#263238"/>
			<Setter Property="Margin" Value="0,4,0,0"/>
		</Style>

		<Style TargetType="GroupBox">
			<Setter Property="BorderBrush" Value="#B2DFDB"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="Background" Value="White"/>
			<Setter Property="Margin" Value="0,0,0,14"/>
			<Setter Property="Padding" Value="0"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="GroupBox">
						<Grid>
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto"/>
								<RowDefinition Height="*"/>
							</Grid.RowDefinitions>

							<Border Grid.Row="0"
                                    Background="#E0F2F1"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1,1,1,0"
                                    CornerRadius="6,6,0,0"
                                    Padding="12,6">
								<ContentPresenter ContentSource="Header"
                                                  RecognizesAccessKey="True"
                                                  TextElement.Foreground="#004D40"
                                                  TextElement.FontWeight="SemiBold"
                                                  TextElement.FontSize="14"/>
							</Border>

							<Border Grid.Row="1"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1,0,1,1"
                                    CornerRadius="0,0,6,6"
                                    Padding="12,8">
								<ContentPresenter/>
							</Border>
						</Grid>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<Style TargetType="TextBlock">
			<Setter Property="Foreground" Value="#37474F"/>
			<Setter Property="FontSize" Value="13"/>
		</Style>
	</Window.Resources>

	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<Border Grid.Row="0"
                Background="White"
                CornerRadius="8"
                Padding="12,10"
                Margin="0,0,0,16"
                Effect="{StaticResource WindowShadow}">
			<StackPanel Orientation="Horizontal">
				<Button Content="📂 Загрузить" Command="{Binding LoadCommand}" Margin="0,0,8,0"/>
				<Button Content="💾 Сохранить" Command="{Binding SaveCommand}"/>
			</StackPanel>
		</Border>

		<TextBlock Grid.Row="1"
                   Text="{Binding Status}"
                   Margin="0,0,0,14"
                   FontWeight="SemiBold"
                   FontSize="14"
                   Foreground="#455A64"/>

		<ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
			<StackPanel>
				<GroupBox Header="🔍 Статус сервиса">
					<StackPanel Orientation="Horizontal" Margin="0,4">
						<Ellipse Width="16"
                                 Height="16"
                                 Margin="0,0,10,0"
                                 VerticalAlignment="Center"
                                 Fill="{Binding ServiceState, Converter={StaticResource ServiceStatusBrushConverter}}"/>
						<TextBlock Text="{Binding ServiceStatusText}"
                                   VerticalAlignment="Center"
                                   FontWeight="SemiBold"
                                   FontSize="14"
                                   Foreground="#263238"/>
					</StackPanel>
				</GroupBox>

				<GroupBox Header="📋 Список ИНН">
					<StackPanel>
						<DockPanel Margin="0,0,0,8">
							<TextBox Width="260"
                                     Text="{Binding NewInn, UpdateSourceTrigger=PropertyChanged}"
                                     Margin="0,0,10,0"/>
							<Button Content="➕ Добавить" Command="{Binding AddInnCommand}" Margin="0,0,6,0"/>
							<Button Content="✖️ Удалить" Command="{Binding RemoveInnCommand}"/>
						</DockPanel>

						<ListBox ItemsSource="{Binding InnList}"
                                 SelectedItem="{Binding SelectedInn}"
                                 MinHeight="120"/>
					</StackPanel>
				</GroupBox>

				<GroupBox Header="⚙️ Настройки">
					<StackPanel>
						<TextBlock Text="Интервал (часы)" Margin="0,0,0,4"/>
						<TextBox Text="{Binding IntervalHours, UpdateSourceTrigger=PropertyChanged}"
                                 Width="120"
                                 HorizontalAlignment="Left"/>

						<TextBlock Text="Email для уведомлений" Margin="0,12,0,4"/>
						<TextBox Text="{Binding SmtpTo, UpdateSourceTrigger=PropertyChanged}"
                                 Width="280"
                                 HorizontalAlignment="Left"/>

						<CheckBox Content="Тестовый режим"
                                  IsChecked="{Binding Test}"
                                  Margin="0,12,0,0"/>
					</StackPanel>
				</GroupBox>
			</StackPanel>
		</ScrollViewer>
	</Grid>
</Window>